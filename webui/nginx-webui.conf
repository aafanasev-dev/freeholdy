# Web UI — serves the React control panel container at https://cloudopen.space
# Manual system config (not generated by freeholdy).
# Install: sudo cp nginx-webui.conf /etc/nginx/sites-available/freeholdy_webui.conf
#          sudo ln -s /etc/nginx/sites-available/freeholdy_webui.conf /etc/nginx/sites-enabled/
#          sudo certbot certonly --nginx -d cloudopen.space   # first issue (HTTP block must be live)
#          sudo nginx -t && sudo nginx -s reload

server {
    listen 80;
    server_name cloudopen.space;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name cloudopen.space;

    ssl_certificate     /etc/letsencrypt/live/cloudopen.space/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cloudopen.space/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    location / {
        proxy_pass         http://127.0.0.1:14173;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}
